CC = gcc
SRC = main.c school.c
OBJ = $(patsubst %.c,%.o,$(SRC))
NAME = school
RM = wiper

all: $(OBJ)
	${CC} ${OBJ} -o ${NAME}

clean:
	rm -f *~ *.swp ${NAME}

oclean:
	rm -f ${OBJ}

fclean:
	rm -f ${NAME} ${OBJ} *~ *.swp

re:
	${CC} ${SRC} -o ${NAME}
